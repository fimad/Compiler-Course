=For Friday
  -expose rand
  -arrays as vectors
  -matrix mult
  -graphs
    -vector optimizations with loop unrolling

=For FUN
  -function section
    -functions have end keyword instead of in
    -must appear after types before main segment
  -type parameters
  -implement lists!!!
  -change let in to let in end
  -merge basic blocks

=For because it would be cool
  -nested functions
    -determine which variables are used in inner scope, and make them present in the stack
    -implicit first parameter to functions which is the static frame pointer
  -classes
